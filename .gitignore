# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# env files (never commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local
# a local copy of the deployed values, kept for reference — never commit it
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# drizzle (keep migrations, ignore local meta journal noise if desired)
# /drizzle is committed — migrations are source of truth

# local Supabase CLI scaffolding (npx supabase init/start) — not part of the app
/supabase/

# generated by `next dev` / `next build` (Next.js 16.3+ agent rules)
/AGENTS.md
/CLAUDE.md
